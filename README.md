# NetflixData_project
End to End Data Engineering Project Using GitHub as the source system. Data ingested- parameterized pipeline with incremental data loading logic to efficiently process only updated records. The architecture follows the Medallion Architecture. data transformation using PySpark in Databricks, Finally the curated data loaded into Azure Synapse.
